# Fathom

Askaig fetches Fathom at configure time from commit
`c9c6fef0dddc05d2e242c183acf5833149ab676d` and verifies the archive SHA-256 in `CMakeLists.txt`.
The local `tbconfig.h` keeps only the probing API and uses compiler popcount/lsb intrinsics.

Source: https://github.com/jdart1/Fathom
